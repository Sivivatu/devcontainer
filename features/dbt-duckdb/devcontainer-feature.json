{
    "id": "dbt-duckdb",
    "version": "1.0.0",
    "name": "dbt-core with DuckDB Adapter",
    "description": "Installs dbt-core and the dbt-duckdb adapter into an isolated virtual environment. Add the duckdb feature separately when you need the DuckDB CLI.",
    "documentationURL": "",
    "options": {
        "dbtCoreVersion": {
            "type": "string",
            "default": "1.9.1",
            "description": "Version of dbt-core to install."
        },
        "dbtDuckdbVersion": {
            "type": "string",
            "default": "1.9.1",
            "description": "Version of dbt-duckdb adapter to install."
        }
    },
    "installsAfter": ["duckdb"],
    "containerEnv": {
        "PATH": "/opt/dbt/bin:${PATH}"
    }
}
